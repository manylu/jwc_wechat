<!DOCTYPE html>
<html>
  <head>
    <title>业务流程</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=0.5, maximum-scale=2.0, user-scalable=yes" />
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/jquery-weui.min.css' />
    <link rel='stylesheet' href='/stylesheets/weui.min.css' />
  </head>
  <body>
  <div class="entry">
    <div class="informPic">
      <img class="picRes" src="../images/inform.jpg" alt="">    
    </div>
    <p class="entryTitle_1"><%= workFlow.title %><p>
    <p class="entryDate"><%= workFlow.date %><p>
    <div class="entryContent">
      <% for(let i = 0;i<workFlow.content.entry.length;i++){ %>
        <p><%= workFlow.content.entry[i] %><p>
      <% } %>

      <table class="tableRe">
        <% for(let j = 0;j<workFlow.content.table.length;j++){ %>
          <tr class="trRe">
            <% if(workFlow.content.table[j].length<workFlow.content.table[0].length) { %>
            <td class="tdRe"></td>
            <% } %>
            <% for(let k=0;k<workFlow.content.table[j].length;k++){ %>
            <td class="tdRe">
              <%= workFlow.content.table[j][k] %>
            </td>
            <% } %>
          </tr>
        <% } %>
      </table>

      <div class="imgs">
        <% for(let j = 0;j<workFlow.content.image.length;j++){ %>
          <img src="<%= workFlow.content.image[j] %>" alt="">
        <% } %>
      </div>

    </div>
 </div>
    
  
  <script src='/javascripts/jquery-2.1.4.js'></script>
  <script src='/javascripts/jquery-weui.min.js'></script>
  </body>
</html>
